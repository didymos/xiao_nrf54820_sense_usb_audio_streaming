# Core (acoustic quality analysis)
numpy>=1.21

# Optional — speech intelligibility (--transcribe). Install one of:
faster-whisper>=1.0        # recommended: fast CPU transcription (int8)
# openai-whisper           # alternative, heavier

# Optional — charts (--plots)
matplotlib>=3.5
